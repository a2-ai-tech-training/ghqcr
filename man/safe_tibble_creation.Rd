% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_app_helpers.R
\name{safe_tibble_creation}
\alias{safe_tibble_creation}
\title{Create a safe tibble with guaranteed schema}
\usage{
safe_tibble_creation(data, schema, map_fn)
}
\arguments{
\item{data}{A list/vector of data to process, or NULL/empty}

\item{schema}{A named list specifying column names and their empty types
(e.g., list(name = character(0), number = integer(0), open = logical(0)))}

\item{map_fn}{A function to apply to each element of data to create a tibble row}
}
\value{
A tibble with guaranteed schema
}
\description{
Creates a tibble with the specified schema, either populated from data or
empty with the correct column types if data is empty.
}
\examples{
\dontrun{
# Safe milestone creation
milestone_df <- safe_tibble_creation(
  milestones,
  schema = list(name = character(0), number = integer(0), open = logical(0)),
  map_fn = function(x) tibble::tibble(
    name = x$title,
    number = x$number,
    open = identical(x$state, "open")
  )
)
}
}
